.jumbotron.jumbotron_fix{style: "background: url('#{asset_path("background/groupcolombia.jpg")}') bottom center no-repeat"}

.jumbotron.jumbotron_fix_mobile{style: "background: url('#{asset_path("background/groupcolombiamobile.jpg")}') bottom center no-repeat"}


.scroll-slow
	#homepage-text 
		#adventure-local-explainer
			We help make adventures, so you can be
		#adventure-local
			made by adventure
		#adventure-local-explainer-mobile
			We help make adventures, so you can be
		#adventure-local-mobile
			made by adventure
		


:javascript
	$(document).ready(function() {
	$('#homepage-text').show();
	$('#homepage-text').animate({ opacity: 1, top: "-60px" }, 800);
	});

	function simpleParallax() {
		var scrolled = $(window).scrollTop() + 1;
		$('.jumbotron_fix').css('background-position', '0' + -(scrolled * 0.3) + 'px');
	}

	$(window).jumbotron_fix(function (e) {
		simpleParallax();
	});

	function simpleParallax() {
		var scrolled = $(window).scrollTop() + 1;
		$('.jumbotron_fix_mobile').css('background-position', '0' + -(scrolled * 0.3) + 'px');
	}

	$(window).jumbotron_fix_mobile(function (e) {
		simpleParallax();
	});